const str = '     Harsh Kaushik    ';

const trim = () => console.log(str.trim());

const changeToUpperCase = () => console.log(str.toUpperCase());

const changetoLowerCase = () => console.log(str.toLowerCase());

module.exports = {trim, changeToUpperCase, changetoLowerCase};